generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
// Restored schema matching applied initial migration (integer IDs)

model User {
  id           Int            @id @default(autoincrement())
  email        String         @unique
  passwordHash String
  name         String?
  createdAt    DateTime       @default(now())
  progress     UserProgress[]
  ctfSolves    CTFSolve[]
}

model Course {
  id           Int            @id @default(autoincrement())
  title        String
  description  String?
  lessons      Lesson[]
  userProgress UserProgress[]
}

model Lesson {
  id       Int     @id @default(autoincrement())
  title    String
  content  String?
  course   Course  @relation(fields: [courseId], references: [id])
  courseId Int
}

model CTFChallenge {
  id          Int    @id @default(autoincrement())
  title       String
  description String?
  flag        String
  difficulty  Int
  solves      CTFSolve[]
}

model CTFSolve {
  id       Int          @id @default(autoincrement())
  user     User         @relation(fields: [userId], references: [id])
  userId   Int
  ctf      CTFChallenge @relation(fields: [ctfId], references: [id])
  ctfId    Int
  solvedAt DateTime     @default(now())
}

model UserProgress {
  id        Int      @id @default(autoincrement())
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
  course    Course   @relation(fields: [courseId], references: [id])
  courseId  Int
  progress  Int      @default(0)
  updatedAt DateTime @updatedAt
}

