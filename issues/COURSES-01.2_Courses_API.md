# COURSES-01.2 — CRUD API для курсов (read-only MVP)

Epic: Core
Priority: Must
Estimate: 4ч

Описание:
- Реализовать endpoints для получения списка курсов и конкретного курса (read-only для MVP).

Критерии приёмки:
- Список курсов возвращается с пагинацией/фильтрацией по необходимости.

Чеклист:
- [ ] GET /api/courses
- [ ] GET /api/courses/:id
